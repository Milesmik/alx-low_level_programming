#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Description: This is a test fiel to evaluate the function (())
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;

	while (num = 0; num <=9; num++)
	{
		putchar(num);
			if (num == 9)
			break;

		putchar(',');
		putchar(' ');
	}

	putchar('\n');

	return (0);
}
