#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num
	while (num = '0'; num <= '9')
	{
		printf("%d \n", num);
		(num++);
	}
	printf("\n");
	return (0);
}

