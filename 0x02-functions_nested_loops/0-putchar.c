#include <stdio.h>

#include <main.h>

/**
 * main - Entry point
 * Description: Write a program that prints _putchar, followed by a new line.
 * Return: Always 0 (success)
 */

int main(void)
{
	int n = _putchar;

	putchar(n);

	putchar('\n');

	return (0);
}
